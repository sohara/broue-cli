<BrewForm
  @title="New Brew"
  @model={{this.model}}
  @styles={{this.styles}}
  @sendSave={{action "sendSave" this.model}}
  @sendCancel={{action "sendCancel"}}
  @resetErrorMessage={{action "resetErrorMessage"}}
/>

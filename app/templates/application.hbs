<div class="container">
  <h2 id='title'>Broue.io</h2>
  {{#if session.isAuthenticated}}
  <a {{ action 'invalidateSession' }}>Logout</a>
  {{else}}
  {{#link-to 'login'}}Login{{/link-to}}
  {{/if}}

  {{outlet}}
</div>
